PROJECT(CURLTEST)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

ADD_EXECUTABLE(curltest main.c)

FIND_PACKAGE(CURL)
IF (CURL_FOUND)
    INCLUDE_DIRECTORIES(${CURL_INCLUDE})
    TARGET_LINK_LIBRARIES(curltest ${CURL_LIBRARY})
ELSE()
    MESSAGE(FATAL_ERROR "curl no found")
ENDIF()
